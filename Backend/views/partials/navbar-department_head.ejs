<nav class="bg-blue-600 text-white p-4">
  <div class="max-w-7xl mx-auto flex justify-between items-center">
    <!-- Title -->
    <h1 class="text-xl font-bold">Department Head Portal</h1>

    <!-- Hamburger (mobile only) -->
    <button
      id="dept-menu-toggle"
      class="sm:hidden focus:outline-none focus:ring-2 focus:ring-white rounded"
    >
      <svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16M4 18h16"
        ></path>
      </svg>
    </button>

    <!-- Desktop Menu -->
    <ul class="hidden sm:flex space-x-6">
      <li>
        <a href="/department_head/dashboard" class="hover:underline"
          >Dashboard</a
        >
      </li>
      <li>
        <a href="/department_head/staff" class="hover:underline">Staff</a>
      </li>
      <li>
        <a href="/department_head/requests" class="hover:underline">Requests</a>
      </li>
      <li><a href="/profile" class="hover:underline">Profile</a></li>
      <li><a href="/logout" class="hover:underline text-red-200">Logout</a></li>
    </ul>
  </div>

  <!-- Mobile Dropdown -->
  <div id="dept-mobile-menu" class="sm:hidden hidden mt-4">
    <ul
      class="fixed inset-0 bg-blue-700 bg-opacity-95 flex flex-col justify-center items-center space-y-6 text-xl z-40 gap-6"
    >
      <button
        id="dept-menu-close"
        class="absolute top-4 right-6 text-white text-3xl"
      >
        &times;
      </button>

      <li>
        <a href="/department_head/dashboard" class="block hover:underline"
          >Dashboard</a
        >
      </li>
      <li>
        <a href="/department_head/staff" class="block hover:underline">Staff</a>
      </li>
      <li>
        <a href="/department_head/requests" class="block hover:underline"
          >Requests</a
        >
      </li>
      <li><a href="/profile" class="block hover:underline">Profile</a></li>
      <li>
        <a href="/logout" class="block hover:underline text-red-200">Logout</a>
      </li>
    </ul>
  </div>
</nav>

<script>
  // Mobile toggle
  document.getElementById("dept-menu-toggle").addEventListener("click", () => {
    const menu = document.getElementById("dept-mobile-menu");
    menu.classList.toggle("hidden");
  });

  document.getElementById("dept-menu-close").addEventListener("click", () => {
    const mobileMenu = document.getElementById("dept-mobile-menu");
    mobileMenu.classList.add("hidden");
  });
</script>
